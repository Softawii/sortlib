#include <stdio.h>
#include <stdlib.h>

/*
 * void *vector: a pointer to the first element of a vector.
 * int n: number of vector elements.
 * int size: size in bytes of a element.
 * int cmp: pointer to a comparative function.
 */
void bubblesort(void *vector, int n, int size, int (*cmp)(const void*, const void*)) {

}
